language: node_js
node_js:
- "stable"
cache:
 directories:
  - node_modules
before_script:
  - psql -c 'create database travis'; -u postgres
after_success:
  - npm run coveralls
after_script:
  - psql -c 'drop database travis'; -u postgres
services:
  - postgresql